<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ttait.domain.user.mapper.UserMapper">

    <resultMap id="userResultMap" type="com.ttait.domain.user.domain.User">
        <id property="id" column="id"/>
        <result property="organizationId" column="organization_id"/>
        <result property="loginId" column="login_id"/>
        <result property="password" column="password"/>
        <result property="name" column="name"/>
        <result property="email" column="email"/>
        <result property="phone" column="phone"/>
        <result property="role" column="role" javaType="com.ttait.domain.user.domain.RoleType"/>
        <result property="status" column="status" javaType="com.ttait.domain.user.domain.UserStatus"/>
        <result property="lastLoginAt" column="last_login_at"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="userColumns">
        id,
        organization_id,
        login_id,
        password,
        name,
        email,
        phone,
        role,
        status,
        last_login_at,
        created_at,
        updated_at
    </sql>

    <select id="findById" parameterType="long" resultMap="userResultMap">
        SELECT <include refid="userColumns"/>
        FROM t_user
        WHERE id = #{id}
    </select>

    <select id="findByLoginId" parameterType="string" resultMap="userResultMap">
        SELECT <include refid="userColumns"/>
        FROM t_user
        WHERE login_id = #{loginId}
    </select>

    <select id="findByEmail" parameterType="string" resultMap="userResultMap">
        SELECT <include refid="userColumns"/>
        FROM t_user
        WHERE email = #{email}
    </select>

    <!-- 특정 기업의 담당자(기업 관리자) 조회 -->
    <select id="findByOrganizationId" parameterType="long" resultMap="userResultMap">
        SELECT <include refid="userColumns"/>
        FROM t_user
        WHERE organization_id = #{organizationId}
          AND role = 'COMPANY_ADMIN'
        FETCH FIRST 1 ROWS ONLY
    </select>

    <!-- 특정 role 의 ACTIVE 유저 전체 조회 (관리자 전원 broadcast 용) -->
    <select id="findAllByRole" resultMap="userResultMap">
        SELECT <include refid="userColumns"/>
        FROM t_user
        WHERE role = #{role}
          AND status = 'ACTIVE'
        ORDER BY id
    </select>

    <insert id="insert" parameterType="com.ttait.domain.user.domain.User">
        <selectKey keyProperty="id" resultType="long" order="BEFORE">
            SELECT user_seq.NEXTVAL FROM dual
        </selectKey>
        INSERT INTO t_user (
            id,
            organization_id,
            login_id,
            password,
            name,
            email,
            phone,
            role,
            status,
            created_at,
            updated_at
        ) VALUES (
            #{id},
            #{organizationId},
            #{loginId},
            #{password},
            #{name},
            #{email},
            #{phone},
            #{role},
            #{status},
            SYSTIMESTAMP,
            SYSTIMESTAMP
        )
    </insert>

    <update id="updateLastLoginAt" parameterType="long">
        UPDATE t_user
        SET
            last_login_at = SYSTIMESTAMP,
            updated_at = SYSTIMESTAMP
        WHERE id = #{id}
    </update>
</mapper>
