<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ttait.domain.admindashboard.mapper.AdminDashboardMapper">

    <!-- 통계 데이터가 존재하는 시작일을 조회해 이전 비교 기간 조회 가능 여부를 판단한다. -->
    <select id="findEarliestUsageStatDate" resultType="java.time.LocalDate">
        SELECT MIN(stat_date)
        FROM t_station_usage_stat
    </select>

    <!-- 통계 데이터가 존재하는 최신일을 조회해 기본 조회 기간의 기준일로 사용한다. -->
    <select id="findLatestUsageStatDate" resultType="java.time.LocalDate">
        SELECT MAX(stat_date)
        FROM t_station_usage_stat
    </select>

    <!-- 승인 대기 중인 협약 신청 건수를 KPI 카드에 표시하기 위해 조회한다. -->
    <select id="countPendingApplications" resultType="int">
        SELECT COUNT(*)
        FROM t_organization
        WHERE agreement_status = 'PENDING'
    </select>

    <!-- 지정 기간의 이용량을 일자와 지역 단위로 집계해 차트와 상위 지역 목록에서 재사용한다. -->
    <select id="findUsageAggregates"
            resultType="com.ttait.domain.admindashboard.dto.projection.AdminUsageAggregateProjection">
        WITH station_daily_usage AS (
            <!-- 같은 날짜/대여소에 여러 행이 있을 수 있어 먼저 대여소 일별 이용량으로 묶는다. -->
            SELECT
                u.stat_date,
                u.station_code,
                SUM(u.usage_count) AS usage_count
            FROM t_station_usage_stat u
            WHERE u.stat_date BETWEEN #{from} AND #{to}
            GROUP BY u.stat_date, u.station_code
        )
        SELECT
            d.stat_date,
            NVL(s.district_name, '미분류') AS region_name,
            SUM(d.usage_count) AS usage_count
        FROM station_daily_usage d
        JOIN t_station s ON s.station_code = d.station_code
        GROUP BY d.stat_date, NVL(s.district_name, '미분류')
        ORDER BY d.stat_date ASC, region_name ASC
    </select>

    <!-- 현재 기간 이용량이 이전 동일 기간보다 증가한 대여소를 배치 우선 검토 후보로 조회한다. -->
    <select id="findPriorityRegionCandidates"
            resultType="com.ttait.domain.admindashboard.dto.projection.AdminPriorityRegionProjection">
        WITH current_station_usage AS (
            <!-- 조회 기간의 대여소별 이용량이다. -->
            SELECT
                u.station_code,
                SUM(u.usage_count) AS current_usage
            FROM t_station_usage_stat u
            WHERE u.stat_date BETWEEN #{from} AND #{to}
            GROUP BY u.station_code
        ),
        previous_station_usage AS (
            <!-- 조회 기간과 같은 길이의 이전 기간 대여소별 이용량이다. -->
            SELECT
                u.station_code,
                SUM(u.usage_count) AS previous_usage
            FROM t_station_usage_stat u
            WHERE u.stat_date BETWEEN #{previousFrom} AND #{previousTo}
            GROUP BY u.station_code
        ),
        priority_candidates AS (
            <!-- 이전 이용량이 있는 대여소 중 10% 이상 증가한 후보만 남긴다. -->
            SELECT
                c.station_code,
                s.station_name,
                c.current_usage,
                NVL(p.previous_usage, 0) AS previous_usage,
                CASE
                    WHEN NVL(p.previous_usage, 0) = 0 AND c.current_usage > 0 THEN 100
                    WHEN NVL(p.previous_usage, 0) = 0 THEN 0
                    ELSE ROUND(((c.current_usage - p.previous_usage) / p.previous_usage) * 100, 1)
                END AS usage_growth_rate
            FROM current_station_usage c
            JOIN t_station s ON s.station_code = c.station_code
            JOIN previous_station_usage p ON p.station_code = c.station_code
            WHERE c.current_usage >= 10
              AND (
                    p.previous_usage > 0
                    AND c.current_usage >= p.previous_usage * 1.1
              )
        )
        SELECT
            rank,
            station_name,
            usage_growth_rate,
            review_status,
            total_count
        FROM (
            SELECT
                ROW_NUMBER() OVER (ORDER BY current_usage DESC, station_name ASC) AS rank,
                station_name,
                usage_growth_rate,
                CASE
                    WHEN usage_growth_rate >= 20 THEN 'REVIEW_REQUIRED'
                    ELSE 'MONITOR'
                END AS review_status,
                COUNT(*) OVER () AS total_count
            FROM priority_candidates
        )
        WHERE rank &lt;= 5
        ORDER BY rank ASC
    </select>

    <!-- 최근 승인 대기 협약 신청 5건을 대시보드 목록에 표시하기 위해 조회한다. -->
    <select id="findPendingApplications"
            resultType="com.ttait.domain.admindashboard.dto.response.AdminPendingApplicationResponse">
        SELECT
            organization_name,
            contact_name AS manager_name,
            CAST(submitted_at AS DATE) AS requested_date,
            agreement_status AS status,
            address AS area_name
        FROM t_organization
        WHERE agreement_status = 'PENDING'
        ORDER BY submitted_at DESC NULLS LAST, id DESC
        FETCH FIRST 5 ROWS ONLY
    </select>
</mapper>
